<Window x:Class="Gitster.Views.CommitDetailsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Commit details"
        SizeToContent="WidthAndHeight"
        MinWidth="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="220"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="SHA" Foreground="{DynamicResource TextSecondary}" Margin="0,0,10,8"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="ShaBox" IsReadOnly="True" BorderThickness="0" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Summary" Foreground="{DynamicResource TextSecondary}" Margin="0,0,10,8"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="SummaryBox" IsReadOnly="True" BorderThickness="0" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Author" Foreground="{DynamicResource TextSecondary}" Margin="0,0,10,8"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="AuthorBox" IsReadOnly="True" BorderThickness="0" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Committer" Foreground="{DynamicResource TextSecondary}" Margin="0,0,10,8"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="CommitterBox" IsReadOnly="True" BorderThickness="0" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Author date" Foreground="{DynamicResource TextSecondary}" Margin="0,0,10,8"/>
        <TextBox Grid.Row="4" Grid.Column="1" x:Name="DateBox" IsReadOnly="True" BorderThickness="0" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="Commit date" Foreground="{DynamicResource TextSecondary}" Margin="0,0,10,0"/>
        <TextBox Grid.Row="5" Grid.Column="1" x:Name="CommitterDateBox" IsReadOnly="True" BorderThickness="0"/>

        <TextBlock Grid.Row="7" Grid.Column="0" Text="Message" Foreground="{DynamicResource TextSecondary}" Margin="0,0,10,0"/>
        <TextBox Grid.Row="7" Grid.Column="1" x:Name="MessageBox"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"/>

        <Button Grid.Row="9" Grid.Column="1"
                Content="Cl_ose"
                Style="{StaticResource DialogButton}"
                HorizontalAlignment="Right"
                IsDefault="True"
                IsCancel="True"
                Click="Close_Click"/>
    </Grid>
</Window>
