<Window x:Class="Gitster.Views.OperationProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Gitster.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:OperationProgressViewModel}"
        Title="{Binding TitleText}"
        Width="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundPrimary}"
        Closing="Window_Closing">
    <Border Padding="18"
            BorderBrush="{DynamicResource BorderTertiary}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="{Binding TitleText}"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimary}"/>

            <ProgressBar Grid.Row="1"
                         Height="8"
                         Margin="0,14,0,14"
                         Minimum="0"
                         Maximum="{Binding ProgressMaximum}"
                         Value="{Binding ProgressValue}"/>

            <StackPanel Grid.Row="2">
                <TextBlock Text="{Binding StageText}"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"/>
                <TextBlock Text="{Binding DetailText}"
                           Margin="0,4,0,0"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource TextSecondary}"/>
            </StackPanel>

            <TextBlock Grid.Row="3"
                       Text="{Binding ElapsedText}"
                       Margin="0,16,0,0"
                       Foreground="{DynamicResource TextTertiary}"
                       FontFamily="{DynamicResource FontFamilyMono}"/>

            <Button Grid.Row="4"
                    Content="Cancel"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0"
                    Padding="12,7"
                    IsEnabled="{Binding CanCancel}"
                    AutomationProperties.Name="Cancel operation"
                    Click="Cancel_Click"/>
        </Grid>
    </Border>
</Window>
