<UserControl x:Class="Gitster.Controls.SelectableTextBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gitster.Controls"
             xmlns:converters="clr-namespace:Gitster.Converters"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=local:SelectableTextBlock}"
             d:DesignHeight="24" d:DesignWidth="32">
    <UserControl.Resources>
        <converters:BoolConverter x:Key="BoolConverterVisible" Type="Visible"/>
        <converters:BoolConverter x:Key="BoolConverterCollapsed" Type="Collapsed"/>
        <Style x:Key="HoverStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Style.Triggers>
                <Trigger Property="Border.IsMouseOver" Value="True">
                    <Setter Property="Border.Background" Value="{DynamicResource BackgroundSecondary}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border x:Name="LayoutRoot" x:FieldModifier="private" Height="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Height}" MinHeight="24" Style="{DynamicResource HoverStyle}" MouseDown="BorderOnMouseDown">
        <Grid>
            <Border Visibility="{Binding IsSelected, Converter={StaticResource BoolConverterVisible}}" Background="{DynamicResource AccentBlue}" CornerRadius="4">
                <TextBlock Text="{Binding Text}" TextAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource BackgroundPrimary}" FontWeight="{Binding FontWeight}" Opacity="{Binding Opacity}"/>
            </Border>
            <TextBlock Text="{Binding Text}" TextAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsSelected, Converter={StaticResource BoolConverterCollapsed}}" FontWeight="{Binding FontWeight}" Opacity="{Binding Opacity}" Foreground="{DynamicResource TextPrimary}"/>
        </Grid>
    </Border>
</UserControl>
