<UserControl x:Class="Gitster.Views.Modes.CommitsModeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Gitster.ViewModels"
             xmlns:controls="clr-namespace:Gitster.Controls"
             d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel}"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="500"/>
            <ColumnDefinition Width="3"/>
            <ColumnDefinition Width="380" MinWidth="320"/>
        </Grid.ColumnDefinitions>

        <!-- Left: commit list -->
        <controls:CommitListView Grid.Column="0" DataContext="{Binding CommitListVM}"/>

        <!-- Splitter -->
        <GridSplitter Grid.Column="1"
                      HorizontalAlignment="Stretch"
                      Background="{StaticResource BorderTertiary}"
                      ResizeBehavior="PreviousAndNext"/>

        <!-- Right: action panel -->
        <StackPanel Grid.Column="2" Margin="10,10,12,10">
            <controls:CommitContextCard  DataContext="{Binding CommitListVM}"/>
            <controls:TimestampEditPanel DataContext="{Binding TimestampEditVM}" Margin="0,8,0,0"/>
            <controls:AuthorPanel        DataContext="{Binding AuthorPanelVM}"   Margin="0,8,0,0"/>
            <controls:QuickActionsPanel  DataContext="{Binding QuickActionsVM}"  Margin="0,8,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
