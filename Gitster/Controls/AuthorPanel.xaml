<UserControl x:Class="Gitster.Controls.AuthorPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Gitster.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:AuthorPanelViewModel}">

    <Border Style="{StaticResource PanelCard}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="52"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="✍"  FontSize="11" Foreground="{StaticResource TextTertiary}"/>
                <TextBlock Text="Author" FontSize="10" Foreground="{StaticResource TextTertiary}" Margin="0,1,0,0"/>
            </StackPanel>

            <!-- Quick-switch author dropdown -->
            <ComboBox Grid.Column="1"
                      IsEditable="True"
                      ItemsSource="{Binding Authors}"
                      DisplayMemberPath="DisplayName"
                      Text="{Binding AuthorText, UpdateSourceTrigger=PropertyChanged}"
                      Height="28"
                      FontSize="12"
                      Foreground="{StaticResource TextPrimary}"
                      BorderBrush="{StaticResource BorderTertiary}"
                      ToolTip="Quick-switch author for next amend. Amend button applies the change."/>

            <!-- Edit authors dialog button -->
            <Button Grid.Column="2"
                    Content="Edit..."
                    Command="{Binding OpenEditAuthorsCommand}"
                    Width="48" Height="28"
                    Margin="4,0,0,0"
                    FontSize="11"
                    ToolTip="Open author editor dialog"
                    Style="{StaticResource GhostButton}"/>
        </Grid>
    </Border>
</UserControl>
