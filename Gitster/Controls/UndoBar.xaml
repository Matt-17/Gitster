<UserControl x:Class="Gitster.Controls.UndoBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Gitster.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:UndoBarViewModel}">

    <!-- A7 — always present. Tinted accent when an undoable op exists, neutral otherwise. -->
    <Border Padding="10,0" Height="32">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource BackgroundSecondary}"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding HasUndoableOperation}" Value="True">
                        <Setter Property="Background" Value="{DynamicResource AccentBlueBackground}"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <DockPanel VerticalAlignment="Center">

            <!-- Right: action buttons -->
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Undo"
                        Command="{Binding UndoCommand}"
                        Style="{StaticResource GhostButton}"
                        Height="24"
                        Padding="10,1"
                        Margin="0,0,4,0"
                        Visibility="{Binding HasUndoableOperation, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <Button Content="Log"
                        Command="{Binding OpenLogCommand}"
                        Style="{StaticResource GhostButton}"
                        Height="24"
                        Padding="10,1"/>
            </StackPanel>

            <!-- Left: description text -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="🕘 " FontSize="12" VerticalAlignment="Center"/>
                <TextBlock VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="{Binding LastOperationText}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasUndoableOperation}" Value="False">
                                    <Setter Property="Foreground" Value="{DynamicResource TextTertiary}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>

        </DockPanel>
    </Border>
</UserControl>
